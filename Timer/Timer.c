/*
 * Timer.c
 *
 *  Created on: Mar 19, 2019
 *      Author: mh
 */

/*#include "../include.h"
#include "Timer.h"

static void Timer_ISR(void)
{
    Chop_tat();
}
/*extern void Config_Timer(void)
{
    SysCtlPeripheralEnable(SYSCTL_PERIPH_TIMER0);
    TimerConfigure(TIMER0_BASE,TIMER_CFG_PERIODIC); // config timer
    TimerLoadSet(TIMER0_BASE,TIMER_A,chu_ki_may-1); // load gia tri vao timer, value la so chu ki may
    TimerIntRegister(TIMER0_BASE,TIMER_A,&Timer_ISR);  // dang ki la co ngat timer, ham Timer_ISR la ham ngat
    IntEnable(INT_TIMER0A);  // cho phep ngat timer 0
    TimerIntEnable(TIMER0_BASE,TIMER_TIMA_TIMEOUT);  //

    TimerEnable(TIMER0_BASE,TIMER_A); //bat timer len
}*/ */
